import { Title } from "./Title/Title";
import { DatePanel } from "./DatePanel/DatePanel";

export const Header = () => {
  return (
    <header>
      <Title />
      <DatePanel />
    </header>
  );
};
